
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand text-success" href="#">S.K-Project</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <% if current_user.role == "user" %>
        
          <li class="nav-item">
              <a class="nav-link text-primary" href="<%= edit_user_path(current_user.id)%>">Update-Profile</a>
          </li>
          <li class="nav-item ">
              <a class="nav-link text-primary" href="<%= new_education_path%>">Create Education</a>
          </li>
        <% else %>
            <li class="nav-item">
                <a class="nav-link text-primary" href="<%= users_path%>">UserList</a>
            </li>
            <li class="nav-item ">
                <a class="nav-link text-primary" href="<%= educations_path%>">EducationList</a>
            </li> 

            <li class="nav-item ">
                <a class="nav-link text-primary" href="<%= new_education_path%>">Create Education</a>
            </li>

            <form class="d-flex" action="/search" method="GET">
              
              <input class="form-control me-2" type="search" placeholder="filter-enter/true/false/all" aria-label="Search"  name="activatedkey">
              <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"  name="key">
              <button class="btn btn-outline-success" type="submit">Search</button>
            </form>

        <% end %>
      </ul>
      <div>
       <p style="color: green "><%= notice %></p>
      </div>
      
      <ul class="nav justify-content-end ">
        <span class="bg-success p-2 ">WELCOME. <%= current_user.first_name %> <%= current_user.last_name %> || <%= current_user.role %></span>
        <li class="nav-item">
      
          <%= link_to "Log Out", destroy_session_sessions_path, data: {turbo_method: :delete}, class: "nav-link btn btn-danger text-success"%>
        </li>
      </ul>
      

    </div>
  </div>
</nav>